class Comment {
    constructor(id, quizId, userId, text) {
      this.id = id;
      this.quizId = quizId;
      this.userId = userId;
      this.text = text;
    }
  }
  
  module.exports = Comment;